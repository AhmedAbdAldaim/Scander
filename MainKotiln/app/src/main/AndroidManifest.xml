<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mainkotiln">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme1">

        <activity android:name=".detiles_order"></activity>
        <activity android:name=".Order" />
        <activity android:name=".chipes" />
        <activity android:name=".chipes_big" />
        <activity android:name=".select_chipess_type" />
        <activity android:name=".Singup" />
        <activity android:name=".Main" />
        <activity android:name=".Login" />
        <activity android:name=".Carts" />
        <activity android:name=".fragmentHome" />
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>